# --- Server Configuration ---
PORT=3000
NODE_ENV=development

# --- Database Configuration (Docker Defaults) ---
# When running via Docker Compose, the DB_HOST is the service name ('db').
# These values match the defaults set in docker-compose.yml.
DB_HOST=db
DB_USER=root
DB_PASSWORD=root
DB_NAME=loja_db

# --- Security (REQUIRED) ---
# You MUST change this secret to a long, random string in production.
# Ideally, generate a random hash (e.g., using 'openssl rand -base64 32').
JWT_SECRET=change_this_secret_to_something_secure